import _import

a = Alpha()

assert.equal(a.getName(), "Alpha")
assert.equal(Alpha.getName(a), "Alpha")

class Bravo(Alpha):
    def __init__(self):
        self.name = "Bravo"
    @staticmethod
    def setName(obj, name):
        obj.name = name

testBravo()

a = foo.Bar()
assert.equal(a.baz(), "foo bar baz")
